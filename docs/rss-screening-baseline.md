# RSS 提示词保护基线

2026-09-20 用户明确要求恢复长期打磨的 RSS 提示词，唯一保留的近期筛选调整是最终入库门槛 6.0。未经用户明确要求，不得改写这套提示词、改变去留规则、评分含义或 QA 契约。

## 恢复依据

- 生产提交 `2d455690f8d026080fd56a4f530fcb895e699655`（2026-09-13）：保留原提示词，恢复最终内容分数门槛，低于 6.0 进入回收站，正好 6.0 保留，包括 LINUX DO。
- `37f5ad656c765f8b90d21c7954c2af49edc02f04` 的父版本中，三个提示词与上述 9 月 13 日版本完全一致。恢复时直接取历史原文，不重新润色。
- 保护文件：`docs/local-screen-triage-prompt.md`、`docs/local-screen-prompt.md`、`docs/local-summarize-prompt.md`。文件 SHA-256 见 `docs/rss-screening-baseline.json`，测试检查实际文件未漂移。

## 已否决，不得继续或 resume

普通 RSS 的“独立增量判断 / value-first / 低增量”路线已否决。不得重新加入 `increment.kind=none` 强制过滤、keep 可被内容处理推翻、用新增价值标准代替原有规则、QA 降至 1 组等改动。不得通过修改校验、重试提示词、注入上下文或同步另一仓库来间接恢复这些行为。

## 恢复后的实际路径

初筛按历史提示词输出 keep/filter/uncertain，原有初筛信号门槛仍为 3.8；keep 在内容处理阶段不能改为 pass；uncertain 按原六类救回 / 五类噪音终审；生成原有评分、分类、标题、摘要、关键词及 QA 后，再统一按最终内容门槛 6.0 决定入库。两个分数是不同阶段的输出，不应把“最终阈值 6.0”误实现为额外收紧初筛。

## 范围与生产真源

本次恢复普通 RSS 的筛选与 QA，不回退抓取、写表可靠性、重试退休或本机 publisher 主触发修复，不顺带调整 Grok 专题采集规则。

实际执行仓库是 `acaiaishizhan/rss-ingest-action`；本地 `F:/coding/rss-ingest-local` 是另一仓库。修复必须核对生产 main 和后续运行使用的提交，不能只改本地文件就宣称生效。历史已过滤记录不会因恢复提示词自动重新处理；补跑须作为明确范围的恢复工作执行。

## 本次验证

- 生产工作副本完整测试：686 passed。覆盖原三态规则、keep 保护、QA 至少 3 组、6.0 边界、LINUX DO 低分回收及后续可靠性修复。
- 真实 Ark 只读复测：今天被低增量过滤的 Anthropic 26% 研发资讯，恢复后输出 ingest / 7.2 分 / 3 组 QA；理由重新按原 Step 2a 类别给出。复测未写飞书，结果不代表已补回历史记录。
